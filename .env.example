# Optional PostgreSQL for live company registry (static SP500 cache works without DB)
# DATABASE_URL=postgresql://user:pass@host:5432/industry

# Static ThroatScan-compatible ticker map (default bundled path)
# GICS_STATIC_MAP_PATH=./data/throatscan-gics-cache.json
# GICS_SEED_PATH=./gics_seed.json

# HTTP server port (Docker / Railway / Render set PORT automatically)
# PORT=8001
